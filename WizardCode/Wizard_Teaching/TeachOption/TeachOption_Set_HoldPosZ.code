// 인클루드
include("\\WizardCode\\Wizard_Teaching\\Teaching_System.h");

VAR CurWorkPos[8]:#30010;
VAR dlgMssg[10];

if( TeachOpHoldPosZOn )
{
	TeachOpHoldPosZ = CurWorkPos[2];

	VAR memIndex;
	memIndex = get_varindex("TeachOpHoldPosZ");
	dlgMssg[0] =	"type=edit, no=2, memory=memIndex, writememory=memIndex,";
	WINC_SET_USER_WINDOW(1, dlgMssg, 1);
}
else
{
	dlgMssg[0] =	"type=edit, no=2, memory=30012, writememory=37902,";
	WINC_SET_USER_WINDOW(1, dlgMssg, 1);
}

beep(2500, 0.2);



